shortname: mass
name: mass
version: 0.1.1
api:
  rest:
    produces:
      public: true
      gateway_prefix: /api/mass
storage:
  mongodb:
    db_name: mass
    mode: read-write
extra_config:
  searchable_classes:
    value_instructions: Searchable artifact or resource types. Check service config schema.